/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['intercocina.com', 'static.everypixel.com', 'cdn-icons-png.flaticon.com', 'boisetblanc.fr', 'readymadeui.com', 'localhost'],
  },
};

export default nextConfig;
